# Novel Craft Rebirth Implementation Plan

Goal: deliver the approved generation-and-revision CLI reset.
Architecture: dependency-free modular Node runtime with immutable revisions and a write-ahead journal; existing Rust code retained.
Tech stack: Node 22.14+, ESM, built-in test runner, GitHub Actions and npm trusted publishing.
Spec: ../specs/2026-09-07-rebirth.md

- [x] Write black-box failing tests for the new CLI JSON envelope, character-free genre defaults, proposals, memory propagation and stale-write protection. Run `node --test cli/test/*.test.mjs` and record expected failure before implementation.
- [x] Implement `cli/store.mjs`, `cli/context.mjs` and `cli/checks.mjs`; prove explicit canon approval survives restart, partial writes recover and stale proposals cannot overwrite edits.
- [x] Implement `cli/catalogue.mjs` with dated source provenance, distinct original examples and no unknown-genre fallback. Test every observed category mapping.
- [x] Implement `cli/runner.mjs` and `cli/generate.mjs`; test real subprocess JSON, malformed responses, timeout, call caps, interrupted resume and proposal-only defaults. Keep deterministic fixture outputs labelled as protocol tests, not quality evidence.
- [x] Implement CLI, reading exports and onboarding skill. Test the advertised workflow, package contents and installed executable in a fresh prefix. Local Node suite: 44 passed; real-model quality remains unverified.
- [ ] Push reviewed source, open a PR, run the preserved Rust CI and new runtime matrix, inspect failures, merge only a known head, trigger the existing authorised release path and read back publication status.
